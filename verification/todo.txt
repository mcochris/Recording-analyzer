- check if setting the threshold to 0 or a negative number breaks a verification program (DONE)

- check if the SoX mp3 libsox-fmt-mp3 extension is installed (DONE)

- check if the python modules used are installed (DONE)

- should the threshold # be a percentage like 1 for 1%, 10 for 10%? Divide % by 100 to get tolerance. 100% needs to be set to 0 tolerance (DONE, using %)

- allow comments in threshold file (DONE, not allowed)

- add readme's to each verification directory (DONE)

- add test files to each verification directory (later)

- modify verification directories to also verify against test files (later)

- check audio file suffix, don't try to process files we can't handle, generate other formats with ffmpeg (done)

- test on WSL (done)

- test on FreeBSD (later)

- can pgms run against NAS files? (depends on NAS)

- does "find . -mindepth 1" work on FreeBSD? (later)

- fix "cut -w" problem (DONE)

- in verify.sh, if you don't give the music file's absolute pathname, when it cd's to another dir the music file won't be found (DONE)

- can't run verify on a file if you're not in the verification dir (can't source common.sh) (DONE)

- limit displayed numbers two 2 decimal places (DONE)

- remove "threshold x%" from messages (except header) (DONE)

- loudgain # for true peak is way off (DONE)

- python divide by 0 on peak level for March...Jim Spence...wav (DONE)

- top 5 audio file formats: mp3, aac, wav, flac, ogg (ok)

- sox can't do aac or m4a (ok)

- allow RE for filename (DONE, restricted to globbing)

- add metadata option (DONE)

- build tabulator website to read in json data (DONE)

- export to spreadsheet format (DONE)

- name playlist files "artist - album.m3u8" (NOPE - playlist may contain multiple artists/albums)

- add "Processing # of # to pgm msg, e.g. Processing file 3 of 10 "Computer Love.flac"... (DONE)

- add -q option for no messages, helps when piping find cmd into pgm in background (DONE)

- playlists only created per tabulator page, can't span pages (FIXED - turned of pagination)

- add find command helper pgm to pipe to recording-analyzer (DONE)

- ffmpeg may fail, need to catch that (DONE)

- merge ez-analyzer into recording-analyzer so there's only one program (DONE)

- incorporate user defined file extensions (DONE)


==============================================================================

- run main program on all my audio files as test

- run verification on all my audio files as a test

- does setting a threshold to 0% break anything?

- does setting a threshold to 100% break anything?

- can threshold be set to -12% or 110%?

- add a FAQ:
	- why does it take so long to process a file?
	- how do i get the pgm into a dir in my $PATH?
	- if i upload my json, will you store any data?
	- do you keep any logs?
	- how do i get to the files on my NAS?
	- which formats do you handle? can i change them?
	- why do i have to upload to your website?

- reasons i made this:
	- eval my recordings for gain levels and noise
	- make custom playlist by genres/dynamic range
	- create spreadsheet of my songs to quickly scan for stuff i'd like to hear
	- find files with missing metadata
	- compare different recordings of same song/album
